interface:
  display_name: "Agent Ticketing OS"
  short_description: "Install or upgrade repo-local agent ticketing"
  default_prompt: "Use $agent-ticketing-os to install or safely upgrade Agent Ticketing OS in this repository."

policy:
  allow_implicit_invocation: true
